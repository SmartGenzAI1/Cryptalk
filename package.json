{
  "name": "cryptalk-scripts",
  "private": true,
  "scripts": {
    "seed": "bun run scripts/seed.ts",
    "db:push": "prisma db push",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
