# Frontend environment variables
# Copy this file to .env.local and customize as needed

# Backend port (for API requests via Caddy gateway)
# The API client automatically appends ?XTransformPort=BACKEND_PORT
BACKEND_PORT=8001

# Next.js
NEXT_PUBLIC_APP_NAME=Cryptalk
